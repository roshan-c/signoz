NODE_ENV="development"
BUNDLE_ANALYSER="true"
FRONTEND_API_ENDPOINT="http://localhost:8080/"
PYLON_APP_ID="pylon-app-id"
APPCUES_APP_ID="appcess-app-id"
PYLON_IDENTITY_SECRET="pylon-identity-secret"

# Base path for serving the frontend from a sub-path (e.g., /signoz/ or /admin/observe/)
# This is used for development only. In production, the base path is injected at runtime
# by the backend based on the web.prefix configuration in the server config.
# Example: BASE_PATH="/signoz/"
BASE_PATH="/"

CI="1"